{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "CRITICAL: Railway Root Directory MUST be '.' (root) not 'frontend'. This config assumes root deployment.",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd shared-types && npm install && npm run build && cd ../frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd frontend && npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}