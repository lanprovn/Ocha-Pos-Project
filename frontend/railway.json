{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Root Directory is set to 'frontend' on Railway. Build shared-types from ../shared-types",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../shared-types && npm install && npm run build && cd ../frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}