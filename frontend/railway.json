{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Root Directory is 'frontend'. Copy shared-types from parent before build.",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/copy-shared-types.cjs && cd shared-types && npm install && npm run build && cd .. && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}