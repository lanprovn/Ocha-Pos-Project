{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "IMPORTANT: Railway must deploy from ROOT of monorepo. Configure Root Directory in Railway Settings to '.' (root)",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=shared-types && npm run build --workspace=frontend"
  },
  "deploy": {
    "startCommand": "cd frontend && npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}