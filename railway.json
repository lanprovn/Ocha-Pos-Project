{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd shared-types && npm install && npm run build && cd ../backend && npm install && npm run build"
  }
}

