{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "IMPORTANT: Railway must deploy from ROOT of monorepo. Configure Root Directory in Railway Settings to '.' (root)",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=shared-types && npm run build --workspace=backend"
  },
  "deploy": {
    "startCommand": "cd backend && npm run prisma:generate && npx prisma migrate deploy && npm run prisma:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

