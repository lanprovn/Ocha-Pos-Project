{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "IMPORTANT: Railway must deploy from ROOT of monorepo, not from backend folder. Configure Root Directory in Railway Settings to '.' (root)",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd shared-types && npm install && npm run build && cd ../backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm run prisma:generate && npx prisma migrate deploy && npm run prisma:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

