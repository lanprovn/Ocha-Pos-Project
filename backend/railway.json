{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:shared || true && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run prisma:generate && npx prisma migrate deploy && npm run prisma:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

