{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Root Directory is 'backend'. Copy shared-types from parent before build.",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/copy-shared-types.js && cd shared-types && npm install && npm run build && cd .. && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run prisma:generate && npx prisma migrate deploy && npm run prisma:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

